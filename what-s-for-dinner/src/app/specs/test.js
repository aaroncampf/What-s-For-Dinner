
let mondayRecipe = String;
describe("Get Monday recipe", () => {
    it("should be a recipe name", () => {
        expect(recipe).toBe(String)
    })

})

let tuesdayRecipe = String;
describe("Get Tuesday recipe", () => {
    it("should be a recipe name", () => {
        expect(recipe).toBe(String)
    })

})

let wednesdayRecipe = String;
describe("Get Wednesday recipe", () => {
    it("should be a recipe name", () => {
        expect(recipe).toBe(String)
    })

})

let thursdayRecipe = String;
describe("Get Thursday recipe", () => {
    it("should be a recipe name", () => {
        expect(recipe).toBe(String)
    })

})
let fridayRecipe = String;
describe("Get Friday recipe", () => {
    it("should be a recipe name", () => {
        expect(recipe).toBe(String)
    })

})

let saturdayRecipe = String;
describe("Get Saturday recipe", () => {
    it("should be a recipe name", () => {
        expect(recipe).toBe(String)
    })

})

let sundayRecipe = String;
describe("Get Sunday recipe", () => {
    it("should be a recipe name", () => {
        expect(recipe).toBe(String)
    })

})